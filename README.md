# DeviceSupportVersions
A simple macOS app that lets you see which device supports files are known to your Xcode. It also lets you delete them directly. Its very useful when trying to recover disk space.
